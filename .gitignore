node_modules/
.next/
dist/
.env.local
.DS_Store
public/models/
.superpowers/
miris/data.json
.playwright-mcp/
tsconfig.tsbuildinfo

# Working directories, not part of the starter attendees clone: the raw kit
# drop, and the notes and specs written while building the guide.
miris-web-kit/
docs/

# Temp test assets copied from /repos/miris-boutique. Not shipped: 25MB of
# binaries, and WebContainer drops binary files on import anyway. The real
# room ships slimmed and materials load from URLs we host.
public/env/
public/props/

# CGAxis assets. Licensed to us for use, NOT for redistribution: their terms
# forbid sharing PBR materials in any form or at any resolution, free or paid,
# and this repo is cloned by every attendee. They stay on local disk so the
# vault build runs here; they must not enter the public mirror. See
# public/tex/vault/SOURCES.md for what they are and how to re-fetch them.
# The CONTENTS, not the directory: git cannot re-include a file whose parent
# directory is excluded, so ignoring the folder itself would take the note with
# it.
public/tex/vault/*
public/props/vault/*
!public/tex/vault/SOURCES.md
